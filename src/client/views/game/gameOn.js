const gameOn = () {
  // game logic here. waits until a player has won

  // get a tile aka peel, triggered by socket

  // dump. exchange 1 for 3 from bank

  // run dictionary check when all tiles are used up, if no pass player loses and waits until a winner
};

export default gameOn;
